# 3.3 环境差异矩阵

> 生成时间: 2026-04-08
> 分析方法: 配置参数 + 硬编码值 + WPS 插件约束分析

---

## 环境差异对比表

| 变量/配置 | 本地开发 | WPS 插件环境 | 差异风险 |
|----------|---------|-------------|---------|
| `APP_HOST` | `127.0.0.1` | `127.0.0.1` | 相同 |
| `APP_PORT` | `8000` | `8000` | 相同 |
| CORS | `allow_origins=["*"]` | WPS 前端来源可能受限 | **中**（WPS 宿主的 origin 可能非标准） |
| 系统代理 | Clash 代理 | 可能有公司代理 | **中**（httpx 默认读系统代理，可能干扰） |
| 文件系统权限 | 完全访问 | WPS 沙箱可能受限 | **高** |
| 数据目录 | `./data/` 相对路径 | 取决于工作目录 | **高** |

---

## 硬编码清单（跨环境风险）

| 硬编码项 | 位置 | 当前值 | 跨环境影响 |
|---------|------|--------|-----------|
| Qdrant 默认路径 | `config.py:151` | `./data/qdrant` | 相对路径，依赖工作目录 |
| SQLite 默认路径 | `config.py:194` | `./data/app.db` | 同上 |
| BM25 索引路径 | `config.py:188` | `./data/bm25_index.json` | 同上 |
| 上传目录 | `library.py:40` | `Path(settings.sqlite_db_path).parent / "uploads"` | 基于 SQLite 路径推导 |
| 论文数据目录 | `library.py:108` | `Path(settings.sqlite_db_path).parent / "papers"` | 基于 SQLite 路径推导 |
| Kimi User-Agent | `kimi_client.py` | `claude-code` | 伪装 UA，WPS 环境可能被 WAF 拦截 |
| httpx timeout | 多处 | 60-120s 硬编码 | 不可通过配置调整 |
| CORS origin | `main.py:14` | `*` | 生产环境需限制 |

---

## WPS 插件环境特有约束

### 已知约束

| 约束 | 影响 | 当前状态 |
|------|------|---------|
| WPS JS API 限制 | 前端只能通过 WPS 提供的 API 与后端通信 | 前端已适配 |
| 文件选择 | 需通过 WPS API 选择文件，非标准 `<input>` | 前端已适配 |
| 窗口嵌入 | 前端运行在 WPS 宿主窗口中 | 已适配 |

### 潜在约束（未验证）

| 约束 | 可能影响 | 验证方法 |
|------|---------|---------|
| **沙箱文件系统** | `./data/` 路径可能无法写入 | 在 WPS 环境中测试导入 |
| **网络代理** | WPS 宿主的系统代理可能影响 httpx | 检查 WPS 环境的 `HTTP_PROXY` |
| **端口冲突** | 8000 端口可能被其他服务占用 | 检查 WPS 环境端口占用 |
| **进程生命周期** | WPS 关闭后后端进程如何管理 | 测试 WPS 关闭后后端是否继续运行 |
| **Content-Security-Policy** | SSE 连接可能被 CSP 阻止 | 检查 WPS 宿主的响应头 |
| **同源策略** | WPS 内嵌浏览器可能有特殊同源策略 | 测试跨域请求 |

---

## 配置差异矩阵（DEV vs PROD）

| 配置项 | DEV | PROD（预期） | 是否需要不同值 |
|--------|-----|-------------|--------------|
| `APP_DEBUG` | `true` | `false` | **是** |
| CORS origins | `*` | WPS 前端 origin | **是** |
| `APP_PORT` | 8000 | 8000 | 否 |
| `QDRANT_PATH` | `./data/qdrant` | 绝对路径 | **是** |
| `SQLITE_DB_PATH` | `./data/app.db` | 绝对路径 | **是** |
| `MINERU_TIMEOUT` | 300 | 300 | 否 |
| `LLM_TEMPERATURE` | 0.7 | 0.7 | 否 |

**【模型推断】** 目前没有 DEV/PROD 环境区分机制，所有配置通过单个 `.env` 文件管理。部署到 WPS 环境时需要手动修改配置。
￥你的优化建议是？￥
￥
【建议】：支持 DEV/PROD 环境区分，通过 `.env.{环境}` 文件管理。

**改动位置**：
- 修改 `config.py`，支持环境变量前缀
- 新增 `.env.dev`、`.env.prod` 文件

**实现方案**：

### 方案 A：环境前缀（推荐）

**改动位置**：`config.py`
```python
import os
from functools import lru_cache

class Settings(BaseSettings):
    # ... 现有字段 ...
    
    model_config = SettingsConfigDict(
        env_file=f".env.{os.getenv('APP_ENV', 'dev')}",  # ← 根据环境加载不同文件
        env_file_encoding="utf-8",
        case_sensitive=False,
    )

@lru_cache
def get_settings() -> Settings:
    return Settings()
```

**使用方式**：
```bash
# 开发环境
export APP_ENV=dev
uv run python main.py  # 加载 .env.dev

# 生产环境
export APP_ENV=prod
uv run python main.py  # 加载 .env.prod
```

**`.env.dev`**：
```env
# 开发环境：本地调试
DEBUG=true
LOG_LEVEL=DEBUG
```

**`.env.prod`**：
```env
# 生产环境：WPS 插件
DEBUG=false
LOG_LEVEL=WARNING
APP_HOST=127.0.0.1
```

---

**优先级**：P2（MVP 后优化）
￥
---

## 进程管理

| 环境 | 后端启动方式 | 进程管理 | 说明 |
|------|-----------|---------|------|
| 本地开发 | `uv run python main.py` | 手动 | Ctrl+C 停止 |
| WPS 插件 | 【未确定】 | 【未确定】 | 需要确定 WPS 如何启动/管理后端进程 |
￥启动serve，可以成熟后通过exe编译好，或者是一个bat脚本一键启动。服务启动后，那个wpsjs debug就会自动加载写的前端的。￥
￥
【确认】：这是一个很好的部署方案建议。

**方案梳理**：

### 方案 A：bat 脚本一键启动（简单）

**改动位置**：新增 `start.bat`
```batch
@echo off
cd /d "%~dp0backend"
echo 启动论文助手后端...
uv run python main.py
pause
```

**使用方式**：
- 用户双击 `start.bat` → 后端启动
- WPS 插件连接到 `http://127.0.0.1:8000`

**优点**：
- ✅ 简单，无需编译
- ✅ 跨平台（Linux/MAC 用 `.sh` 脚本）

**缺点**：
- ❌ 需要 Python 环境 + 依赖安装
- ❌ 用户可以看到控制台（可能误关）

---

### 方案 B：EXE 编译（复杂）

**改动位置**：新增 `build_exe.py`
```python
import PyInstaller

PyInstaller([
    '--name=论文助手',
    '--onefile',
    '--windowed',
    '--icon=icon.ico',
    'main.py',
])
```

**使用方式**：
- 用户双击 `论文助手.exe` → 后端启动
- 后台运行，无控制台窗口

**优点**：
- ✅ 用户无需安装 Python
- ✅ 后台运行，用户体验好

**缺点**：
- ❌ 编译后文件大（~200 MB）
- ❌ 编译复杂，需要打包所有依赖
- ❌ 可能被杀毒软件误报

---

### 方案 C：混合方案（推荐）

**开发阶段**：使用 `start.bat`
**生产阶段**：使用 PyInstaller 编译 EXE

**实现步骤**：
1. 开发期：用 `start.bat` 调试
2. 发布期：用 PyInstaller 打包
3. 用户安装：提供 `论文助手.exe` + `start.bat`（调试用）

---

**WPS 插件集成**：
- WPS 插件启动时 → 检测后端是否运行
- 如果未运行 → 自动启动 `论文助手.exe`
- 如果已运行 → 直接连接

**优先级**：P1（影响用户安装体验）
￥
**【代码事实】** 当前 `main.py` 是标准 `uvicorn` 启动，无 daemon 模式、无 PID 文件、无健康检查重启机制。
