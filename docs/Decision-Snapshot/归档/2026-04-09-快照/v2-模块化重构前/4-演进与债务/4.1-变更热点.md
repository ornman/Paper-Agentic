# 4.1 变更热点

> 生成时间: 2026-04-08
> 分析方法: git log --since="6 months ago" --name-only

---

## Git 基本数据

| 指标 | 值 |
|------|-----|
| 提交数（6 个月内） | 13 |
| 作者数 | 1 (Coro0) |
| 项目起始日期 | 2026-03-23 |
| 最近提交日期 | 2026-04-08 |
| 项目年龄 | ~16 天 |

**说明**：项目处于极早期，变更频率数据不具备统计意义（3 次变更 = 最高频）。以下分析作为基线记录，供后续迭代对比。

---

## 文件变更频率排行（Top 20）

| 变更次数 | 文件路径 | 分类 |
|---------|---------|------|
| 3 | `app/services/retrieval_service.py` | 服务层 |
| 3 | `app/services/qa_service_rag.py` | 服务层 |
| 3 | `app/clients/kimi_client.py` | 客户端层 |
| 3 | `app/api/v1/routes/library.py` | 路由层 |
| 2 | `tests/conftest.py` | 测试 |
| 2 | `app/services/ingest_workflow.py` | 服务层（旧） |
| 2 | `app/services/indexing_service.py` | 服务层（旧） |
| 2 | `app/services/cleaning_service.py` | 服务层（旧） |
| 2 | `app/repositories/chroma_repo.py` | 存储层（旧） |
| 2 | `app/repositories/__init__.py` | 存储层 |
| 2 | `app/processing/describer.py` | 处理层 |
| 2 | `app/modules/indexing/service.py` | 模块层（新） |
| 2 | `app/modules/indexing/dto.py` | 模块层（新） |
| 2 | `app/modules/indexing/chunkers/parent_child.py` | 模块层（新） |
| 2 | `app/modules/indexing/chunkers/brute.py` | 模块层（新） |
| 2 | `app/modules/indexing/chroma_repo.py` | 模块层（新） |
| 2 | `app/modules/indexing/bm25_repo.py` | 模块层（新） |
| 2 | `app/modules/indexing/__init__.py` | 模块层（新） |
| 2 | `app/models/query.py` | 模型层 |
| 2 | `app/core/config.py` | 核心层 |

---

## 高频变动文件风险评估

| 文件 | 变更次数 | 风险评估 |
|------|---------|---------|
| `retrieval_service.py` | 3 | 分布式检索 + RRF 融合，核心逻辑最复杂的文件（412 行），检索策略和 Collection 扫描逻辑仍在调整 |
| `qa_service_rag.py` | 3 | RAG 问答主链路，集成检索和流式输出，与前端联调频繁修改 |
| `kimi_client.py` | 3 | VLM+LLM 双职责客户端，可能因 Kimi API 行为变化而调整 |
| `library.py` | 3 | PDF 导入路由，接口契约可能随产品需求变化 |

---

## 僵尸代码候选列表

项目仅 16 天，无 6 个月未变更文件。以下为疑似冗余/过时文件：

| 文件 | 状态 | 说明 |
|------|------|------|
| `app/services/cleaning_service.py` | 旧版 | 已迁移到 `modules/ingestion/cleaning.py` |
| `app/services/ingest_workflow.py` | 旧版 | 已迁移到 `services/ingest_service.py` |
| `app/services/indexing_service.py` | 旧版 | 已迁移到 `modules/indexing/` |
| `app/repositories/chroma_repo.py` | 旧版 | 已从 ChromaDB 迁移到 Qdrant |
| `app/modules/indexing/chroma_repo.py` | 旧版 | 同上，modules 内的 ChromaDB 残留 |
| `app/workflows/__init__.py` | 空文件 | 预留未使用 |
| `clients/llm_client.py` 的 `LLMClient` | 命名冲突 | 与 `vlm_client.py` 中的抽象类同名但无继承关系 |
￥之后更新完再重构吧，这个标记可以之后统一清理￥
￥
【确认】：技术债务已记录，待路由切换到 `modules/` 层后统一清理。

**清理清单**（待执行）：
1. 删除旧版服务：
   - `app/services/indexing_service.py` → 已迁移到 `modules/indexing/`
   - `app/repositories/chroma_repo.py` → 已迁移到 Qdrant

2. 清理残留：
   - `app/modules/indexing/chroma_repo.py` → ChromaDB 残留
   - `app/workflows/__init__.py` → 空文件

3. 解决命名冲突：
   - `clients/llm_client.py` 的 `LLMClient` → 重命名为 `OpenAILLMClient`
   - 或改为继承 `vlm_client.py` 的抽象类

**触发条件**：路由切换到 `modules/` 层，且运行稳定 1 周后

**优先级**：P2（暂缓）
￥
---

## `🔮 未来扩展` 标记统计

共 **17 处**标记，集中在 3 个文件中：

| 文件 | 标记数 | 内容 |
|------|--------|------|
| `models/query.py` | 6 | `resource_types` 和 `selected_papers` 参数定义 |
| `api/v1/routes/query.py` | 3 | 路由层接口预留 |
| `services/qa_service_rag.py` | 3 | RAG 问答中的检索范围控制 |
| `services/retrieval_service.py` | 5 | Collection 过滤逻辑骨架 |

**扩展点密度**：17 处标记 / 5989 行代码 ≈ 每 352 行 1 个未完成扩展点。

所有扩展点指向同一功能：**用户自选文献检索**。
