# 4-答应了什么 — 契约/测试

## 勾子正文

### 后端契约（代码证据）

#### 1. 配置硬约束（app/core/config.py:8-12,286-298）

**断言**：Embedding/Rerank 模型与维度固定，系统启动时强制校验

**证据**：
- `PINNED_EMBEDDING_MODEL = "Qwen/Qwen3-Embedding-8B"` (config.py:10)
- `PINNED_EMBEDDING_DIMENSIONS = 1536` (config.py:11)
- `PINNED_RERANK_MODEL = "Qwen/Qwen3-Reranker-8B"` (config.py:12)
- `@model_validator` 校验逻辑在 config.py:286-298，不匹配抛出 `ValueError`

**测试覆盖**：tests/core/test_config_contract.py:22-39

---

#### 2. 错误统一契约（app/core/errors.py:9-126）

**断言**：所有领域错误必须包含 `code`/`stage`/`message`/`detail` 字段

**证据**：
- `DomainError` 基类定义（errors.py:33-91）
- 六大阶段错误：`ConfigError`/`IngestionError`/`IndexingError`/`RetrievalError`/`QAError`（errors.py:93-126）
- `__reduce__` 方法确保 pickle/deepcopy 稳定（errors.py:67-82）

**测试覆盖**：tests/core/test_error_contract.py:8-113

---

#### 3. API 响应信封格式（app/models/base.py）

**断言**：所有 API 响应使用 `ApiResponse[data, code, message]` 统一包装

**证据**：
- 所有路由返回类型标注为 `ApiResponse[...]`（router.py:5-11, 各路由文件）
- 前端 `ApiResponseEnvelope` 对应字段（api-client.ts:1-5）

**测试覆盖**：需人工补充（当前测试未直接验证响应信封）

---

#### 4. 文档状态机（app/modules/library/models.py）

**断言**：文档状态必须按 `pending → parsing → cleaning → indexing → completed` 推进

**证据**：
- `DocumentRecord.transition()` 方法（models.py, 行号需读取）
- 拒绝非法状态转移（如 completed → indexing）

**测试覆盖**：tests/library/test_document_state_machine.py:34-44

---

#### 5. 本地路径安全边界（app/modules/library/service.py）

**断言**：PDF 导入只接受本地绝对路径，拒绝 URL/UNC/非 PDF 文件

**证据**：
- `import_pdf()` 路径校验（service.py, 行号需读取）
- 测试用例显式拒绝 `https://`、`//server/`、`.txt`（test_ingestion_pipeline.py:457-502）

**测试覆盖**：tests/ingestion/test_ingestion_pipeline.py:457-521

---

#### 6. MinerU 同源策略（app/modules/ingestion/mineru_client.py）

**断言**：只接受同源或白名单 CDN 的 `result_url`，拒绝外部地址

**证据**：
- 同源判断逻辑（mineru_client.py, 行号需读取）
- CDN 白名单：`cdn-mineru.openxlab.org.cn`（test_ingestion_pipeline.py:251-287）
- 拒绝 `evil.example.com`（test_ingestion_pipeline.py:380-412）

**测试覆盖**：tests/ingestion/test_ingestion_pipeline.py:167-412

---

#### 7. 双索引一致性（app/modules/indexing/service.py）

**断言**：Chroma 与 BM25 必须同时写入成功，任一失败则回滚

**证据**：
- `index_document()` 事务逻辑（service.py, 行号需读取）
- BM25 写失败时回滚 Chroma（test_index_modes.py:566-593）
- 重建失败时保留旧索引（test_index_modes.py:451-489）

**测试覆盖**：tests/indexing/test_index_modes.py:355-593

---

#### 8. Embedding 维度守卫（app/modules/indexing/service.py）

**断言**：写入前校验 `model_name` 和 `dimensions`，不匹配则拒绝

**证据**：
- `_validate_embedding_contract()` 方法（service.py, 行号需读取）
- 测试用例拒绝 `BAAI/bge-m3` 和 1024 维（test_index_modes.py:323-352）

**测试覆盖**：tests/indexing/test_index_modes.py:323-352

---

#### 9. 前端 API 白名单（frontend/src/services/api-client.ts:44-108）

**断言**：API 基地址只允许 `127.0.0.1`/`localhost`/`[::1]`，拒绝外部主机

**证据**：
- `LOCAL_API_HOSTNAME_ALLOWLIST`（api-client.ts:44）
- `normalizeConfiguredApiBaseUrl()` 校验逻辑（api-client.ts:76-108）
- 拒绝协议相对地址 `//host`（api-client.ts:82-85）

**测试覆盖**：需人工补充（当前无前端测试）

---

#### 10. SSE 流式契约（frontend/src/services/sse-client.ts:167-394）

**断言**：`/api/v1/query/ask` 返回 SSE 流，支持 `chunk`/`sources`/`done`/`error` 事件

**证据**：
- `parseSseFrame()` 解析逻辑（sse-client.ts:167-195）
- `readSseStream()` 事件分发（sse-client.ts:287-361）
- 后端路由定义（query.py:23-87）

**测试覆盖**：需人工补充（当前无前端测试）

---

### 前端契约（代码证据）

#### 1. 会话 Store 不可变性（frontend/src/stores/conversation.ts:77-85,243-255）

**断言**：Store 对外暴露的数据必须不可变，更新采用拷贝模式

**证据**：
- `cloneSources()` 显式拷贝（conversation.ts:77-85）
- `updateAssistantMessage()` 返回新数组（conversation.ts:243-255）

**测试覆盖**：需人工补充（当前无前端测试）

---

#### 2. Session ID 隔离（frontend/src/stores/conversation.ts:104,447-448）

**断言**：每次 `reset()` 必须生成新 `session_id`，确保后端历史隔离

**证据**：
- `createSessionId()` 使用 `crypto.randomUUID()`（conversation.ts:61-69）
- `reset()` 中刷新 `sessionId`（conversation.ts:447-448）

**测试覆盖**：需人工补充（当前无前端测试）

---

#### 3. 来源卡片映射（frontend/src/services/sse-client.ts:219-273）

**断言**：后端字段 `{document, content, page}` 映射到前端 `{title, snippet, page}`

**证据**：
- `normalizeSourceCard()` 映射逻辑（sse-client.ts:219-273）
- 兼容 `title`/`snippet` 和 `document`/`content` 两种字段名（sse-client.ts:245-257）

**测试覆盖**：需人工补充（当前无前端测试）

---

### 未覆盖/需补充

1. **前端 API 客户端测试**：api-client.ts 无测试
2. **前端 Store 状态机测试**：conversation.ts 无测试
3. **前端 SSE 解析器测试**：sse-client.ts 无测试
4. **API 响应信封格式测试**：后端无统一响应格式测试
5. **并发安全性测试**：双索引/状态机无并发测试
6. **性能契约测试**：无超时/吞吐量测试

---

## 勾子质量自检

- [x] 每个断言有代码路径:行号证据
- [x] 未覆盖部分标为"需人工补充"
- [x] 无感性评价（全部基于代码事实）
- [x] 无显而易见内容（只记录非平凡的契约）
