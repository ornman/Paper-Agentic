# 论文助手项目 - 架构分析报告

> 生成时间: 2026-04-08
> 架构状态: **已完成重构 + RRF 优化 + 扩展接口预留**

---

## 一、当前数据流架构（已实现）

### 1.1 PDF 导入与处理流程

```
┌──────────────┐
│ 用户上传 PDF │
└──────┬───────┘
       ▼
┌─────────────────────────────────────────────────────────┐
│           MinerU API (远程解析服务)                      │
│  • 返回 JSON 结构 (content_list_v2.json)                 │
│  • 返回图片文件 (images/)                                │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：数据清洗层 (Cleaning Service)            │
│  • 过滤噪声类型 (page_header/footnote/aside/number)      │
│  • UI 噪声过滤 (Expand table、Copilot 占位符等)         │
│  • LaTeX 清洗 (去除格式命令，保留内容)                   │
│  • 表格 HTML → 管道文本                                  │
│  • 追踪 section 层级                                      │
│  • 输出: Chunk[] (包含图片路径)                          │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：VLM 图片描述 (Describer Service)         │
│  • 调用 Kimi Coding API (kimi-for-coding 模型)          │
│  • 伪装请求头: User-Agent: claude-code                  │
│  • 10 并发请求，带缓存                                    │
│  • 输出: Chunk[] (content 包含图片详细描述)              │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：混合切分策略 (Chunker Service)           │
│  • 优先语义切分 (按 section/paragraph)                   │
│  • 打包策略：两个不超过 32k 的语义块合并                 │
│  • 如果超过 32k：                                        │
│    - 单个不超过 → 分开，不用重叠                         │
│    - 单个超过 → 平均切分到 24k，首尾重叠接近 32k        │
│  • 目标: 接近 24k/chunk，留 8k 缓冲做首尾重叠           │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：Embedding 服务                           │
│  • 调用硅基流动 Qwen3-Embedding-8B (1536维)             │
│  • 批量处理 (batch_size=32)                              │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：Qdrant 向量库存储（分布式）              │
│  • 每篇论文独立 collection                               │
│  • 支持多模态资源扩展（paper_xxx, video_xxx, note_xxx） │
│  • 存储向量 + 元数据 (图片路径、描述、section 等)        │
└────────────────────┬────────────────────────────────────┘
                     ▼
              ┌─────────────┐
              │ 导入完成    │
              └─────────────┘
```

### 1.2 RAG 问答流程（分布式 + RRF 融合）

```
┌──────────────┐
│ 用户提问     │
└──────┬───────┘
       ▼
┌─────────────────────────────────────────────────────────┐
│           后端：Query Rewrite Service                    │
│  • 检测查询类型 (4种场景)                                 │
│  • LLM 改写优化查询                                       │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：Retrieval Service (分布式检索)           │
│  ┌─────────────────────────────────────────────────┐    │
│  │  1. 并行搜索所有 Collection                       │    │
│  │  • paper_xxx, video_xxx, note_xxx...            │    │
│  │  • asyncio.gather 并发调用                       │    │
│  └─────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────┐    │
│  │  2. 跨 Query RRF 融合                             │    │
│  │  • 分数无关，只看排名                             │    │
│  │  • 公式: score += 1/(k + rank + 1)              │    │
│  │  • 不同 Collection 的结果公平融合                 │    │
│  └─────────────────────────────────────────────────┘    │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：Rerank 服务                              │
│  • 调用硅基流动 Qwen3-Reranker-8B                        │
│  • 结果重排序                                             │
│  • 选取 Top-K 相关片段                                    │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           后端：QA Service (LLM)                         │
│  • 构建 Prompt (查询 + 上下文 + 图片描述)                │
│  • 流式输出 (SSE)                                        │
│  • 返回答案 + 来源 + 图片路径                            │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│           前端：展示结果                                 │
│  • 答案渲染                                               │
│  • 来源卡片 (带图片预览)                                  │
│  • 图片路径 → WPS 宿主环境显示                            │
└─────────────────────────────────────────────────────────┘
```

---

## 二、后端分层架构（最新）

```
┌─────────────────────────────────────────────────────────┐
│                    API 层 (FastAPI)                      │
│  /api/v1/health, /session, /query, /config, /library    │
│  • 请求验证 • 响应封装 • 异常处理                         │
│  • 🔮 未来扩展：resource_types, selected_papers 参数    │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                 Services (业务逻辑层)                   │
│  ┌─────────────────────────────────────────────────┐    │
│  │  • qa_service_rag.py         (RAG 问答编排)      │    │
│  │    └─ 🔮 resource_types, selected_papers       │    │
│  │  • ingest_service.py        (导入编排)           │    │
│  │  • retrieval_service.py     (分布式检索 + RRF)   │    │
│  │    └─ 🔮 并行搜索所有 Collection                │    │
│  │  • query_rewrite_service.py (Query 改写)         │    │
│  │  • embedding_service.py     (Embedding 服务)     │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                Processing (处理层)                       │
│  ┌─────────────────────────────────────────────────┐    │
│  │  • cleaner.py        (MinerU JSON 清洗)          │    │
│  │  • describer.py      (VLM 图片描述)              │    │
│  │  • chunker.py        (混合切分策略)              │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                 Stores (数据存储层)                     │
│  ┌─────────────────────────────────────────────────┐    │
│  │  • qdrant_store.py     (分布式向量库)            │    │
│  │    └─ 每篇论文/资源独立 Collection               │    │
│  │  • bm25_store.py       (关键词索引)             │    │
│  │  • sqlite_repo.py      (元数据)                 │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│            Clients (外部服务 + 完整抽象接口)              │
│  ┌─────────────────┬──────────────┬───────────────────┐ │
│  │  MinerU Client  │ Kimi Client  │ SiliconFlow Client│ │
│  │  (PDF 解析)     │ (VLM/LLM)    │ (Embed/Rerank)   │ │
│  └─────────────────┴──────────────┴───────────────────┘ │
│  ┌─────────────────────────────────────────────────┐    │
│  │  vlm_client.py     (完整抽象接口)               │    │
│  │    ├── VLMClient                              │    │
│  │    ├── LLMClient                              │    │
│  │    ├── EmbeddingClient    🆕                   │    │
│  │    └── RerankClient       🆕                   │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

---

## 三、关键技术实现（更新）

### 3.1 完整抽象接口设计（可替换）

```python
# app/clients/vlm_client.py
class VLMClient(ABC):
    """VLM 客户端抽象接口."""
    @abstractmethod
    async def describe_image(image_path, prompt, max_tokens) -> str: ...


class LLMClient(ABC):
    """LLM 客户端抽象接口."""
    @abstractmethod
    async def chat(messages, max_tokens, temperature) -> str: ...
    @abstractmethod
    async def chat_stream(messages, max_tokens, temperature): ...


class EmbeddingClient(ABC):      # 🆕 新增
    """Embedding 客户端抽象接口."""
    @abstractmethod
    async def embed(texts: list[str]) -> list[list[float]]: ...
    @abstractmethod
    async def embed_single(text: str) -> list[float]: ...
    @property
    @abstractmethod
    def dimensions(self) -> int: ...
    @property
    @abstractmethod
    def model_name(self) -> str: ...


class RerankClient(ABC):         # 🆕 新增
    """Rerank 客户端抽象接口."""
    @abstractmethod
    async def rerank(
        query: str,
        documents: list[str],
        top_k: int = 10,
    ) -> list[tuple[int, float]]: ...


# 当前实现
# ├── kimi_client.py: KimiVLMClient, KimiLLMClient
# ├── embedding_client.py: SiliconFlowEmbeddingClient  🆕
# └── rerank_client.py: SiliconFlowRerankClient       🆕
```

### 3.2 分布式 RRF 融合（优化）

```python
# app/services/retrieval_service.py
def _reciprocal_rank_fusion(
    *result_lists: list[tuple[str, float]],
    k: int = 60,
) -> list[tuple[str, float]]:
    """RRF 融合算法（分数无关，只看排名）.

    核心思想：
    - 不同 Collection 的分数可能不可比
    - RRF 只看排名：第 1 名得分 1/(k+1)，第 2 名得分 1/(k+2)...
    - 公式: score[doc_id] += 1 / (k + rank + 1)

    示例：
        list1 = [("doc1", 0.9), ("doc2", 0.8)]  # collection A
        list2 = [("doc2", 0.95), ("doc3", 0.7)]  # collection B
        # doc1: 只在 list1 排第1 → 1/(60+1) = 0.0164
        # doc2: 在 list1 排第2，在 list2 排第1 → 1/(60+2) + 1/(60+1) = 0.0325
        # doc3: 只在 list2 排第2 → 1/(60+2) = 0.0161
        # 结果: doc2 (0.0325) > doc1 (0.0164) > doc3 (0.0161)
    """
    scores: dict[str, float] = {}

    for result_list in result_lists:
        for rank, (doc_id, _) in enumerate(result_list):
            scores[doc_id] = scores.get(doc_id, 0) + 1 / (k + rank + 1)

    return sorted(scores.items(), key=lambda x: x[1], reverse=True)


async def _parallel_search_all_collections(
    self,
    query_vector: list[float],
    limit_per_collection: int,
) -> list[list[tuple[str, float]]]:
    """并行搜索所有 Collection.

    • asyncio.gather 并发调用
    • 失败隔离：单个 Collection 出错不影响其他
    """
    collections = self.qdrant_store.client.get_collections()
    tasks = [
        self._search_single_collection(col, query_vector, limit)
        for col in collections.collections
    ]
    results = await asyncio.gather(*tasks, return_exceptions=True)
    return [r for r in results if not isinstance(r, Exception)]
```

### 3.3 删除功能（完善）

```python
# app/api/v1/routes/library.py
@router.delete("/papers/{paper_id}")
async def delete_paper(paper_id: str):
    """删除论文（包括 Qdrant collection 和文件系统数据）.

    删除内容：
    1. Qdrant 向量库中的 collection
    2. 文件系统中的论文数据目录（MinerU 解析结果、图片、缓存等）
    """
    store = _get_store()

    # 1. 删除 Qdrant collection
    store.delete_paper(paper_id)

    # 2. 删除文件系统中的论文数据
    papers_dir = Path(settings.sqlite_db_path).parent / "papers" / paper_id
    if papers_dir.exists():
        shutil.rmtree(papers_dir)

    return ApiResponse(message=f"论文 {paper_id} 已删除")
```

---

## 四、🔮 未来扩展接口（已预留）

### 4.1 用户自选文献功能

**位置**: `app/services/retrieval_service.py`, `app/models/query.py`

**接口**:
```python
async def retrieve(
    query: str,
    resource_types: list[str] | None = None,    # 🆕 资源类型过滤
    selected_papers: list[str] | None = None,  # 🆕 用户选择论文
) -> dict[str, Any]:
    """
    🔮 未来扩展：Collection 过滤逻辑

    # 1. 按资源类型过滤
    if resource_types:
        collections = [
            c for c in all_collections
            if any(c.startswith(f"{t}_") for t in resource_types)
        ]

    # 2. 按用户选择的论文过滤
    if selected_papers:
        collections = [c for c in collections if c in selected_papers]

    产品价值：
    - 提高准确性：用户知道答案在哪些文献里
    - 增强掌控感：用户主动选择，而非被动接受
    - 减少干扰：排除不相关的文献
    - 提升黏性：用户参与决策过程

    UI 示例：
    ┌─────────────────────────────────────────┐
    │  📚 选择要参考的文献                     │
    │  ☑ 论文1: Attention Is All You Need     │
    │  ☐ 论文2: BERT: Pre-training...         │
    │  ─────────────────────────────────────  │
    │  [已选 1 篇]  [清空选择]                 │
    └─────────────────────────────────────────┘
    """
```

**快速定位所有扩展点**:
```bash
grep -r "🔮 未来扩展" app/
```

---

## 五、技术栈总览

### 5.1 后端

| 类别 | 技术 | 用途 |
|------|------|------|
| 框架 | FastAPI | Web 服务 |
| Python | 3.13+ | 运行时 |
| 向量库 | Qdrant | 分布式向量存储 |
| 关键词检索 | BM25 + jieba | 关键词索引 |
| 元数据存储 | SQLite | 会话/配置 |
| PDF 解析 | MinerU API | 远程解析 |
| VLM | Kimi Coding (kimi-for-coding) | 图片描述 |
| LLM | Kimi Coding / DeepSeek | 问答/改写 |
| Embedding | 硅基流动 Qwen3-Embedding-8B | 向量化 (1536维) |
| Rerank | 硅基流动 Qwen3-Reranker-8B | 重排序 |

### 5.2 前端

| 类别 | 技术 | 用途 |
|------|------|------|
| 框架 | Vue 3 | UI 框架 |
| 语言 | TypeScript | 类型安全 |
| 构建工具 | Vite | 打包 |
| 状态管理 | Pinia | 状态 |
| 部署 | WPS 插件 | 宿主环境 |

---

## 六、配置约束（硬契约）

### 6.1 固定模型常量

```python
# app/core/config.py
PINNED_EMBEDDING_MODEL = "Qwen/Qwen3-Embedding-8B"
PINNED_EMBEDDING_DIMENSIONS = 1536  # 固定
PINNED_RERANK_MODEL = "Qwen/Qwen3-Reranker-8B"
```

**更换模型会导致向量库失效，需要重建索引。**

---

## 七、API 端点清单

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/api/v1/health` | GET | 健康检查 | ✅ |
| `/api/v1/session` | POST | 创建会话 | ✅ |
| `/api/v1/session` | GET | 会话列表 | ✅ |
| `/api/v1/query/ask` | POST | 流式问答（RAG）| ✅ |
| `/api/v1/query/retrieve` | POST | 纯检索 | ✅ |
| `/api/v1/library/import` | POST | 导入 PDF | ✅ |
| `/api/v1/library/papers` | GET | 论文列表 | ✅ |
| `/api/v1/library/papers/{id}` | GET | 论文详情 | ✅ |
| `/api/v1/library/papers/{id}` | DELETE | 删除论文 | ✅ |

---

## 八、分布式架构决策记录

**为什么选分布式（每篇论文一个 Collection）？**

| 需求 | 集中式 | 分布式 |
|------|--------|--------|
| 频繁更新/删除 | ❌ 复杂（按 id 删点） | ✅ 简单（删 collection） |
| 论文数量 ≥ 2000 | ❌ 单 collection 过大 | ✅ 天然隔离 |
| 多模态资源 | ❌ 混在一起 | ✅ 按类型隔离 |
| 知识图谱扩展 | ❌ 需要重构 | ✅ Collection 作为节点 |

**最终选择：分布式**

**RRF 融合决策**：
- 问题：不同 Collection 的分数可能不可比
- 方案：RRF 只看排名，不看分数
- 公式：`score += 1 / (k + rank + 1)`

---

## 附录：参考实现

- Novel_Agents RAG 工具: `D:/真项目/Novel_Agents/.tools/rag`
- z_ai-mcp-server: `D:/开发区/L2Demo/z_ai-mcp-server-0.1.3`
