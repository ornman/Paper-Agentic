# 6-变了什么 — 差分视图

首次快照，无对比基线。

当前代码状态快照：
- 后端 71 个 Python 文件，前端 25 个文件
- `services_archive/` 目录存在，说明经历过一轮服务层重构
- config.py 有完整的旧字段兼容层，说明配置字段经历了重命名（llm_* → deepseek_*）
- .env.example 里有 Chroma 相关配置（`CHROMA_PERSIST_DIR`），但 pyproject.toml 没有chromadb 依赖，说明向量库从 Chroma 迁移到了 Qdrant

**下次快照时对比这个基线。**
