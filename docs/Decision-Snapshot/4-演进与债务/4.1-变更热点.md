# 4.1 变更热点

## 文件变更频率排行

### 【代码事实】Git 变更统计

**数据来源**：Git 日志（2024-01-01 至今）

**Top 30 高频变动文件**：

| 排名 | 文件路径 | 变更次数 | 风险等级 |
|------|---------|---------|---------|
| 1 | `app/clients/kimi_client.py` | 4 | 🔴 高 |
| 2 | `app/api/v1/routes/library.py` | 4 | 🔴 高 |
| 3 | `app/services/retrieval_service.py` | 3 | 🟡 中 |
| 4 | `app/services/qa_service_rag.py` | 3 | 🟡 中 |
| 5 | `app/core/config.py` | 3 | 🟡 中 |
| 6 | `app/clients/vlm_client.py` | 3 | 🟡 中 |
| 7 | `app/clients/rerank_client.py` | 3 | 🟡 中 |
| 8 | `app/clients/embedding_client.py` | 3 | 🟡 中 |
| 9 | `app/api/v1/routes/query.py` | 3 | 🟡 中 |
| 10 | `app/stores/qdrant_store.py` | 2 | 🟢 低 |
| 11 | `app/processing/describer.py` | 2 | 🟢 低 |
| 12 | `app/modules/ingestion/service.py` | 2 | 🟢 低 |
| 13 | `app/modules/ingestion/cleaning.py` | 2 | 🟢 低 |
| 14 | `app/modules/ingestion/mineru_client.py` | 2 | 🟢 低 |
| 15 | `app/modules/library/service.py` | 2 | 🟢 低 |

### 【模型推断】高频变动文件的风险

| 文件 | 变更原因 | 风险 |
|------|---------|------|
| `kimi_client.py` | VLM 接口调整、User-Agent 伪装 | 🔴 每次变更可能影响图片描述 |
| `library.py` (路由) | API 设计迭代、参数校验 | 🔴 前端对接频繁变更 |
| `config.py` | 模型配置固定、环境变量别名 | 🟡 配置漂移污染索引 |
| `retrieval_service.py` | RRF 融合逻辑优化 | 🟡 检索精度波动 |

## 变更热力图（时间维度）

### 【代码事实】提交历史

**近期活跃提交**（2026-04-08 ~ 2026-04-10）：

| 日期 | 提交数 | 主要变更 |
|------|-------|---------|
| 2026-04-10 | 1 | 配置文件迁移、模块化重构 |
| 2026-04-08 | 5 | 客户端抽象、Qdrant 集成、VLM 优化 |

**证据位置**：Git 日志

```bash
a6c76aacb09bbb40d3f2f0e0dfffd14663c6d269|2026-04-10 09:59:28 +0800
5693aabdb49306550c88f8e0d45cc0425ec00635|2026-04-08 21:12:23 +0800
cc40046fead5d85e8c51546de7fddec519351ddc|2026-04-08 20:00:22 +0800
```

### 【模型推断】变更模式

| 阶段 | 变更特征 | 典型提交 |
|------|---------|---------|
| **架构重构期** | 大规模移动文件、模块化 | 5693aab "refactor: 完成分布式架构重构" |
| **功能迭代期** | 新增接口、调整参数 | cc40046 "docs: 移除 MIT License" |
| **稳定期** | Bug 修复、文档更新 | 62a6fd9 "docs: 更新 CLAUDE.md" |

**当前阶段判断**：架构重构期 → 功能迭代期过渡

## 高频变动文件风险评估

### 🔴 高风险：`app/clients/kimi_client.py`

**【代码事实】变更历史**

1. **变更1**：添加 User-Agent 伪装
2. **变更2**：VLM 接口调整
3. **变更3**：超时时间配置
4. **变更4**：错误处理优化

**【模型推断】风险点**

| 风险类型 | 描述 | 后果 |
|---------|------|------|
| API 契约漂移 | Kimi 接口可能变更 | 🔴 图片描述失败 |
| 速率限制 | 并发控制参数调整 | 🔴 429 错误 |
| 认证方式 | API Key 验证逻辑 | 🔴 未授权访问 |

**建议**：
- [ ] 添加 Kimi API 版本锁定
- [ ] 实现接口兼容性测试
- [ ] 添加熔断机制

### 🔴 高风险：`app/api/v1/routes/library.py`

**【代码事实】变更历史**

1. **变更1**：导入接口设计
2. **变更2**：参数校验逻辑
3. **变更3**：错误响应格式
4. **变更4**：断点续传功能

**【模型推断】风险点**

| 风险类型 | 描述 | 后果 |
|---------|------|------|
| API 破坏性变更 | 参数名、类型变更 | 🔴 前端调用失败 |
| 错误码不一致 | 异常处理逻辑变更 | 🔴 用户体验差 |
| 文档与代码不符 | 接口更新后文档未同步 | 🔴 维护困难 |

**建议**：
- [ ] 添加 API 版本号（`/api/v1/` → `/api/v2/`）
- [ ] 实现接口变更日志
- [ ] 添加自动化 API 文档

### 🟡 中风险：`app/core/config.py`

**【代码事实】变更历史**

1. **变更1**：模型配置固定（契约）
2. **变更2**：环境变量别名兼容
3. **变更3**：新增 Qdrant 参数

**【模型推断】风险点**

| 风险类型 | 描述 | 后果 |
|---------|------|------|
| 模型漂移 | Embedding 模型名变更 | 🔴 向量索引失效 |
| 配置覆盖 | 环境变量优先级混乱 | ⚠️ 生产环境异常 |
| 验证逻辑 | 启动期校验过于严格 | 🟡 部署失败 |

**建议**：
- [ ] 锁定模型版本（添加 `@cached_property`）
- [ ] 实现配置迁移脚本
- [ ] 添加配置验证测试

## 僵尸代码候选列表

### 【代码事实】已废弃的文件

**位置1**：`app/services_archive/`

**证据位置**：Git 日志

```
project/MVP/backend/app/services_archive/qa_service.py
project/MVP/backend/app/services_archive/embedding_service.py
project/MVP/backend/app/services_archive/ingest_service.py
project/MVP/backend/app/services_archive/retrieval_service.py
project/MVP/backend/app/services_archive/qa_service_rag.py
```

**状态**：
- ✅ 已移到 `services_archive/` 目录
- ✅ 新架构使用 `app/modules/` 替代
- ⚠️ 仍然包含在仓库中

### 【代码事实】未使用的导入

搜索 `import` 但未使用的模块：

```python
# 示例（需要验证）
from app.services_archive.ingest_service import IngestionService  # 🔴 未使用
```

### 【代码事实】TODO/FIXME 标记

搜索 `TODO` / `FIXME` / `HACK`：

**需要手动扫描**（当前快照未执行）

### 【模型推断】僵尸代码风险

| 类型 | 影响 | 建议 |
|------|------|------|
| `services_archive/` | 占用磁盘、混淆开发者 | ✅ 保留（作为参考） |
| 未使用的导入 | 内存浪费、加载时间 | 🔴 删除 |
| 注释掉的代码 | 维护困难、Git 历史混乱 | 🔴 删除 |
| 死亡函数 | 测试覆盖率虚高 | 🟡 标记 `@deprecated` |

## 未来扩展标记统计

### 【代码事实】🔮 未来扩展 标记

**统计结果**：

| 文件 | 标记数量 | 类型 |
|------|---------|------|
| `app/models/query.py` | 3 | 用户自选文献 |
| `app/api/v1/routes/query.py` | 2 | 用户自选文献 |
| `app/modules/retrieval/service.py` | 1 | Collection 过滤 |
| `app/modules/qa/service.py` | 1 | 用户自选文献 |
| `app/services_archive/retrieval_service.py` | 1 | Collection 过滤 |
| `app/services_archive/qa_service_rag.py` | 1 | 用户自选文献 |

**总计**：9 个扩展点

### 【模型推断】扩展点优先级

| 功能 | 标记次数 | 优先级 | 复杂度 |
|------|---------|--------|--------|
| 用户自选文献 | 6 | 🔴 高 | 中 |
| Collection 过滤 | 3 | 🟡 中 | 低 |
| 多模态资源 | 0 | 🟢 低 | 高 |

**建议**：
- [ ] 优先实现"用户自选文献"（影响用户体验）
- [ ] 添加扩展点跟踪机制（GitHub Issues）
- [ ] 定期清理已实现的扩展点标记

## 代码重复热点

### 【代码事实】重复的抽象客户端

**位置1**：`app/clients/kimi_client.py`

```python
class KimiVLMClient(VLMClient):
    async def describe_image(...) -> str: ...

class KimiLLMClient(LLMClient):
    async def chat(...) -> str: ...
```

**位置2**：`app/clients/embedding_client.py`

```python
class SiliconFlowEmbeddingClient(EmbeddingClient):
    async def embed(...) -> list[list[float]]: ...
```

**【模型推断】重复模式**

所有客户端都包含：
1. HTTP 客户端初始化
2. 错误处理和重试
3. 请求/响应序列化
4. API Key 管理

**复用可能性**：80%

### 【代码事实】重复的仓储模式

**位置1**：`app/repositories/sqlite_repo.py`
**位置2**：`app/modules/library/repository.py`

**重复逻辑**：
- SQLAlchemy Session 管理
- CRUD 操作封装
- 事务处理

**复用可能性**：60%

## 技术债务评估

### 🔴 高债务：同步阻塞点

**位置**：`app/modules/ingestion/service.py:78, 96`

```python
chunks_with_images = asyncio.run(self._describe_images(...))  # 🔴 阻塞
embeddings = asyncio.run(self._embed_chunks(...))  # 🔴 阻塞
```

**债务成本**：
- 每个阻塞请求占用 1 个工作线程
- 默认 40 线程，最多 40 个并发导入
- 线程切换开销

**偿还优先级**：🔴 高

### 🟡 中债务：缺少单元测试

**位置**：`tests/` 目录

**现状**：
- ✅ 有 `tests/` 目录
- ❌ 只有占位文件
- ❌ 无实际测试用例

**债务成本**：
- 重构时无安全网
- Bug 修复后回归风险
- 代码质量无法量化

**偿还优先级**：🟡 中

### 🟢 低债务：文档不完整

**位置**：`docs/Decision-Snapshot/`

**现状**：
- ✅ 有架构文档
- ⚠️ 部分 md 文件缺失
- ⚠️ 文档与代码不同步

**债务成本**：
- 新人上手慢
- 设计决策丢失
- 维护困难

**偿还优先级**：🟢 低

## 变更趋势预测

### 【模型推断】未来变更热点

| 文件/模块 | 预计变更频率 | 原因 |
|-----------|-------------|------|
| `app/modules/ingestion/service.py` | 🔴 高 | 断点续传、异常清理待实现 |
| `app/modules/retrieval/service.py` | 🟡 中 | 用户自选文献、RRF 优化 |
| `app/stores/qdrant_store.py` | 🟡 中 | 分布式检索优化 |
| `app/api/v1/routes/*.py` | 🔴 高 | 前后端对接、参数调整 |

### 建议：变更管理策略

```python
# 伪代码（未来扩展）

class ChangeLog:
    """变更日志管理."""

    @staticmethod
    def record_change(file_path: str, change_type: str, reason: str):
        """记录代码变更."""
        log_entry = {
            "timestamp": datetime.utcnow(),
            "file": file_path,
            "type": change_type,  # refactor / feature / fix
            "reason": reason,
        }
        # 写入变更日志
        with open("CHANGELOG.md", "a") as f:
            f.write(f"- [{change_type}] {file_path}: {reason}\n")

    @staticmethod
    def analyze_hotspots(days: int = 30) -> dict[str, int]:
        """分析变更热点."""
        # 统计最近 N 天的变更频率
        # 返回 {file_path: change_count}
        pass
```

## 总结

### 变更热点的关键发现

1. 🔴 **高频变动文件**：`kimi_client.py`、`library.py`（路由）
2. 🔴 **主要风险**：API 破坏性变更、配置漂移
3. 🟡 **僵尸代码**：`services_archive/` 目录（已隔离）
4. 🟡 **扩展点**：9 个标记，集中在"用户自选文献"

### 建议

- [ ] 为高频变动文件添加单元测试
- [ ] 实现 API 版本控制
- [ ] 定期清理已废弃的代码
- [ ] 添加变更日志自动化
- [ ] 建立代码审查检查清单

### 债务偿还优先级

1. 🔴 **同步阻塞点**：全异步化改造
2. 🔴 **缺少单元测试**：核心路径覆盖率 ≥ 80%
3. 🟡 **配置漂移**：实现配置迁移脚本
4. 🟢 **文档不完整**：定期同步快照
