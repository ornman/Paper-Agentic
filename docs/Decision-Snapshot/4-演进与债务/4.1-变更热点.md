# 4.1 变更热点

> **生成时间**: 2026-04-09  
> **分析方法**: Git 历史分析 + 文件变更统计  
> **数据范围**: 2026-03-23 ~ 2026-04-09（项目前18天）

---

## Git 基本数据

| 指标 | 值 |
|------|-----|
| **提交总数** | 13 |
| **作者数** | 1 (Coro0) |
| **项目起始日期** | 2026-03-23 |
| **最近提交日期** | 2026-04-08 |
| **项目年龄** | ~18 天 |
| **总代码行数** | ~7,487 行 Python |

**说明**: 项目处于极早期（MVP 阶段），变更频率数据尚不具备长期统计意义。以下分析作为基线记录，供后续迭代对比。

---

## 高频变动文件排行（Top 15）

| 变更次数 | 文件路径 | 分类 | 说明 |
|---------|---------|------|------|
| **3** | `retrieval_service.py` | 服务层 | 分布式检索 + RRF 融合，核心逻辑复杂 |
| **3** | `qa_service_rag.py` | 服务层 | RAG 问答主链路，与前端联调频繁 |
| **3** | `kimi_client.py` | 客户端层 | VLM+LLM 双职责，API 行为调整 |
| **3** | `library.py` | 路由层 | PDF 导入接口，契约变化 |
| **2** | `query.py` | 模型层 | `🔮 未来扩展` 标记预留 |
| **2** | `conftest.py` | 测试 | 测试配置调整 |
| **2** | `vlm_client.py` | 客户端层 | 抽象接口优化 |
| **2** | `config.py` | 核心层 | 环境变量配置更新 |
| **2** | `ingest_service.py` | 服务层 | 导入流程迭代 |

**数据来源**: `git log --name-only --since="3 months ago" | sort | uniq -c | sort -rn`

---

## 高风险变动文件分析

### 1. `retrieval_service.py`（变更 3 次）

**风险等级**: 🔴 **HIGH**

**变更原因**:
- 分布式检索 + RRF 融合逻辑复杂（~412 行）
- Collection 扫描策略在调整
- 新增 `selected_papers`/`resource_types` 扩展点

**当前状态**: 
- 已重构为 `modules/retrieval/service.py`
- 旧版保留在 `services_archive/`

**遗留问题**:
```python
# D:/真项目/论文助手/project/MVP/backend/app/modules/retrieval/service.py:45
# 🔮 未来扩展：Collection 过滤逻辑
# if selected_papers:
#     collections = [f"paper_{pid}" for pid in selected_papers]
```

### 2. `qa_service_rag.py`（变更 3 次）

**风险等级**: 🟡 **MEDIUM**

**变更原因**:
- RAG 问答流程集成检索和流式输出
- 与前端 SSE 联调
- 错误处理和降级策略调整

**当前状态**: 已迁移到 `modules/qa/service.py`

---

## 僵尸代码候选列表

### 已确认的冗余文件

| 文件 | 状态 | 迁移目标 | 清理优先级 |
|------|------|---------|-----------|
| `services/cleaning_service.py` | ❌ 旧版 | `modules/ingestion/cleaning.py` | P2 |
| `services/ingest_workflow.py` | ❌ 旧版 | `modules/ingestion/service.py` | P2 |
| `services/indexing_service.py` | ❌ 旧版 | `modules/indexing/service.py` | P2 |
| `repositories/chroma_repo.py` | ❌ 旧版 | Qdrant 替代 | P2 |
| `modules/indexing/chroma_repo.py` | ❌ 残留 | 已迁移 Qdrant | P2 |
| `services_archive/retrieval_service.py` | ⚠️ 归档 | `modules/retrieval/service.py` | P3 |
| `services_archive/qa_service_rag.py` | ⚠️ 归档 | `modules/qa/service.py` | P3 |

### 命名冲突问题

| 文件 | 问题 | 建议方案 |
|------|------|---------|
| `clients/llm_client.py` 的 `LLMClient` | 与 `vlm_client.py` 的抽象类同名 | 重命名为 `OpenAILLMClient` |

### 空目录/文件

- `workflows/__init__.py`: 预留但未使用
- `modules/retrieval/`: 新架构目录（部分实现）
- `modules/qa/`: 新架构目录（部分实现）

---

## `🔮 未来扩展` 标记统计

### 标记分布

| 文件 | 标记数 | 具体位置 |
|------|--------|---------|
| `models/query.py` | **6** | 行 17, 43, 47, 87, 95, 98 |
| `api/v1/routes/query.py` | **2** | 行 28, 95 |
| `modules/retrieval/service.py` | **1** | 行 45 |
| `modules/qa/service.py` | **1** | 行 103 |
| `services_archive/retrieval_service.py` | **1** | 行 239 |
| `services_archive/qa_service_rag.py` | **1** | 行 97 |

**总计**: **12 处**（不含文档注释中的引用）

### 扩展点密度

```
12 处标记 / 7,487 行代码 ≈ 每 624 行 1 个未完成扩展点
```

**评价**: MVP 阶段合理，未发现过度标记。

### 扩展点主题分析

所有扩展点指向**同一功能**：

> **用户自选文献检索**（`selected_papers` + `resource_types`）

**实现路线图**:
1. ✅ 接口预留（`AskRequest`, `RetrieveRequest`）
2. ⏳ Collection 过滤逻辑骨架（`modules/retrieval/service.py:45`）
3. ⏳ 前端 UI 选择器
4. ⏳ 端到端测试

---

## 代码重复热点

### 重复模式识别

| 重复模式 | 位置 | 影响 |
|---------|------|------|
| `httpx.AsyncClient(timeout=X)` | 所有 `*_client.py` | 超时值硬编码，连接无法复用 |
| `get_settings()` 调用 | 几乎每个文件 | 配置单例获取重复 |
| `raise_for_status()` | 所有客户端 | HTTP 错误处理模式分散 |
| MinerU 客户端 | `clients/mineru_client.py` + `modules/ingestion/mineru_client.py` | **两份实现并存** |

### 重复度评估

```
重复代码行数 ≈ 200 行
总代码行数 = 7,487 行
重复率 ≈ 2.7%
```

**评价**: 重复率较低，主要存在于客户端层（可接受）。

---

## 重构历史分析

### 主要重构事件

| 日期 | 提交 | 影响范围 | 变更量 |
|------|------|---------|--------|
| 2026-04-08 | `5693aab` | 分布式架构重构 + RRF 优化 | +1,731 / -1,745 |
| 2026-04-08 | `b026608` | 服务层重构 + 抽象接口 | +759 / -2,216 |
| 2026-04-08 | `11007c4` | 后端架构重构 - 数据流与切分策略 | 新增 1,907 行 |

### 重构特点

1. **高频重构**：18 天内 3 次大重构（平均 6 天 1 次）
2. **方向正确**：从单体服务 → 模块化架构
3. **技术债累积**：新旧代码并存，清理滞后

---

## 结论与建议

### 当前状态

✅ **优势**:
- 项目年轻，代码质量可控
- 已建立抽象接口体系（`VLMClient`, `LLMClient` 等）
- 结构化错误体系完善（`DomainError` 子类）

⚠️ **风险**:
- 新旧架构并存（`services/` vs `modules/`）
- 12 处 `🔮 未来扩展` 标记未实现
- 部分客户端未纳入抽象体系（`MinerUClient`）

### 优先级建议

**P0（立即）**:
- [ ] 统一路由到 `modules/` 层
- [ ] 删除 `services_archive/` 旧代码

**P1（本周）**:
- [ ] 实现 `selected_papers` Collection 过滤
- [ ] 清理 `clients/` 命名冲突

**P2（下周）**:
- [ ] `MinerUClient` 纳入抽象体系
- [ ] 删除僵尸代码文件

---

**数据来源**:
- Git Log: `git log --stat --since="3 months ago"`
- 文件统计: `git log --name-only --since="3 months ago" | sort | uniq -c`
- 代码行数: `find app/ -name "*.py" -exec wc -l {} +`
