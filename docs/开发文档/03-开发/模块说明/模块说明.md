# 模块说明 - 论文助手（重构版 v2.0）

> 版本: v2.0
> 更新日期: 2026-04-18
> 这不是旧代码的补丁，是重写。旧代码可复用的部分见各模块说明。

---

## 1. 新项目结构

```
backend/
├── main.py                        # 启动入口
├── pyproject.toml
├── .env                           # 配置（不提交 git）
├── data/
│   ├── zvec_db/                   # Zvec 向量库存储
│   ├── bm25_index/                # BM25 索引
│   └── app.db                     # SQLite 元数据
│
└── app/
    ├── main.py                    # FastAPI 应用（路由注册、生命周期）
    │
    ├── api/v1/                    # API 路由层
    │   ├── router.py              # 总路由注册
    │   └── routes/
    │       ├── import_routes.py   # POST /import/start, GET /import/status/{id}
    │       ├── query_routes.py    # POST /query, POST /query/preview
    │       ├── paper_routes.py    # GET/DELETE /papers
    │       ├── conversation_routes.py  # GET/POST/DELETE /conversations
    │       ├── poll_routes.py     # POST /poll/sync
    │       ├── health_routes.py   # GET /health
    │       └── config_routes.py   # GET /config
    │
    ├── core/                      # 基础设施
    │   ├── config.py              # .env 加载 + 配置类
    │   ├── errors.py              # 统一错误码 + 异常类
    │   ├── logging_config.py      # 日志配置
    │   └── retry.py               # 重试/降级装饰器
    │
    ├── clients/                   # 外部服务客户端（每个服务一个文件）
    │   ├── llm_client.py          # LLM 对话（DeepSeek 等，LangChain ChatOpenAI 封装）
    │   ├── vlm_client.py          # Kimi VLM（图片理解，需伪造 UA）
    │   ├── embedding_client.py    # 硅基流动 Embedding
    │   └── mineru_client.py       # MinerU PDF 解析 API
    │
    ├── pipelines/                 # 业务管道（核心逻辑）
    │   ├── ingestion/             # 数据清洗管道
    │   │   ├── cleaner.py         # 噪音过滤（正则）
    │   │   ├── image_handler.py   # 图片语义理解（调 VLM）
    │   │   ├── chunker.py         # 语义切块（可复用旧代码）
    │   │   └── service.py         # 管道编排：解析→过滤→图片→切块→向量化→入库
    │   │
    │   └── retrieval/             # 检索问答管道（三层架构）
    │       ├── source_understanding.py  # Layer 1: 意图识别 + 分源理解
    │       ├── query_generation.py      # Layer 2: Dense/BM25 双通道查询生成
    │       ├── fusion.py                # Layer 3: 加权融合 + 去重
    │       └── service.py               # 管道编排：理解→生成→召回→融合→拼上下文→LLM
    │
    ├── services/                  # 业务服务（组合管道 + 存储）
    │   ├── conversation_service.py  # 对话管理（Redis 读写、滑动窗口）
    │   └── paper_service.py         # 论文 CRUD（SQLite + Zvec 联动）
    │
    ├── stores/                    # 存储层（纯数据访问，不含业务逻辑）
    │   ├── zvec_store.py          # Zvec 单 Collection 操作
    │   ├── bm25_store.py          # BM25 + jieba 索引
    │   ├── sqlite_repo.py         # SQLite 操作
    │   └── redis_cache.py         # Redis 操作
    │
    └── models/                    # 数据模型（Pydantic）
        ├── paper.py               # Paper, ImportTask
        ├── query.py               # QueryRequest, QuerySchema, SourceItem
        ├── conversation.py        # Message, Conversation
        └── config.py              # ConfigItem
```

---

## 2. 各模块说明

### 2.1 api/v1/routes/ — API 路由

**职责**: 请求校验、调用 service、格式化响应。不含业务逻辑。

| 文件 | 接口 | 核心逻辑 |
|------|------|----------|
| import_routes.py | POST /import/start | 校验路径 → 调用 ingestion pipeline → 返回 task_id |
| | GET /import/status/{id} | 查询 import_logs |
| query_routes.py | POST /query (SSE) | 解析输入源 → 调用 retrieval pipeline → 流式返回 |
| | POST /query/preview | 只跑 Layer 1+2，不调 LLM |
| paper_routes.py | GET/DELETE /papers | paper_service CRUD |
| conversation_routes.py | GET/POST/DELETE /conversations | conversation_service CRUD |
| poll_routes.py | POST /poll/sync | 写入 Redis 缓存 |

### 2.2 clients/ — 外部服务客户端

**职责**: 封装外部 API 调用，处理认证、重试、错误转换。

| 文件 | 封装服务 | 关键点 |
|------|----------|--------|
| llm_client.py | DeepSeek 等（OpenAI 兼容） | LangChain ChatOpenAI，用户自配 URL/Key |
| vlm_client.py | Kimi Coding API | 三个必需 Header（UA 伪造），仅图片理解 |
| embedding_client.py | 硅基流动 | Qwen3-Embedding-8B，1536 维，批量处理 |
| mineru_client.py | MinerU API | PDF → MD + JSON，超时降级 |

**设计原则**: 每个客户端只负责一个外部服务，接口简单（调用→返回结果）。不关心业务上下文。

### 2.3 pipelines/ingestion/ — 数据清洗管道

**职责**: PDF 导入全流程。

```
service.py（编排）
  ├─ mineru_client.py → PDF 解析
  ├─ cleaner.py       → 噪音过滤
  ├─ image_handler.py → 图片理解（调 vlm_client）
  ├─ chunker.py       → 语义切块
  ├─ embedding_client → 向量化
  └─ zvec_store       → 入库
```

| 文件 | 职责 | 可复用旧代码 |
|------|------|-------------|
| cleaner.py | 正则过滤噪音，保留 MD+JSON 双格式 | `processing/cleaner.py` 基础规则 |
| image_handler.py | 提取图片 → 调 VLM → 标签附加 | 新写 |
| chunker.py | 语义切块（章节级），>32k 均分+重叠 | `processing/chunker.py` 核心逻辑 |
| service.py | 编排整个流程，记录 import_logs | 新写 |

### 2.4 pipelines/retrieval/ — 检索问答管道

**职责**: 三层检索架构的实现。

```
service.py（编排）
  ├─ source_understanding.py  → Layer 1: 意图 + 分源
  ├─ query_generation.py      → Layer 2: Dense/BM25 查询
  ├─ bm25_store + zvec_store  → 双路召回
  ├─ fusion.py                → Layer 3: 融合去重
  ├─ conversation_service     → 拼对话历史
  └─ llm_client               → 流式生成
```

| 文件 | 职责 | 说明 |
|------|------|------|
| source_understanding.py | 识别 intent，为每个 source 生成 dense_query + sparse_query | prompt→意图提取，selection→概念聚焦，draft→缺口检测 |
| query_generation.py | 为 Dense 和 BM25 生成各自的查询 | Dense 用自然语言，BM25 用关键词+实体 |
| fusion.py | source 内融合 → 跨 source 融合 → 去重 → Top-K | per_source_cap, per_doc_cap, weighted_rrf |
| service.py | 编排三层 + 拼 context + 调 LLM | 全流程 |

### 2.5 services/ — 业务服务

| 文件 | 职责 |
|------|------|
| conversation_service.py | Redis 对话 CRUD、滑动窗口管理、token 估算截断 |
| paper_service.py | 论文列表/详情/删除（SQLite 查 + Zvec delete_by_filter 联动） |

### 2.6 stores/ — 存储层

| 文件 | 封装 | 关键操作 |
|------|------|----------|
| zvec_store.py | Zvec 单 Collection | insert, query(filter), delete_by_filter, fetch |
| bm25_store.py | jieba + BM25 索引 | build_index, search(top_k), add_document, remove_document |
| sqlite_repo.py | SQLite | papers/chunks/import_logs/config 四表 CRUD |
| redis_cache.py | Redis | conversation 读写（LIST）、poll 缓存（STRING）、TTL 管理 |

### 2.7 models/ — 数据模型

Pydantic v2 模型，用于请求校验和响应序列化。

| 文件 | 模型 |
|------|------|
| paper.py | Paper, ImportTask, ImportStatus |
| query.py | QueryRequest, QuerySchema, SourceItem, FusionConfig |
| conversation.py | Message, Conversation |
| config.py | ConfigItem |

### 2.8 core/ — 基础设施

| 文件 | 职责 |
|------|------|
| config.py | pydantic-settings 加载 .env，暴露配置对象 |
| errors.py | 统一错误码（1000-5999），自定义异常类，全局异常处理器 |
| logging_config.py | structlog 或标准 logging 配置 |
| retry.py | 指数退避重试装饰器，用于 MinerU/Embedding/LLM 调用 |

---

## 3. 依赖方向

```
routes → services → pipelines → clients
                 ↘ stores ↗
routes → models（所有层共享）
core（所有层共享）
```

**禁止**: clients 直接依赖 stores，stores 直接依赖 pipelines。

---

## 4. 启动流程

```python
# main.py
app = FastAPI(lifespan=lifespan)

async def lifespan(app):
    # 1. 加载配置 (.env → config)
    # 2. 初始化 Zvec (create_and_open / open)
    # 3. 初始化 SQLite (建表)
    # 4. 初始化 Redis 连接
    # 5. 初始化 BM25 索引
    # 6. 注册路由
    yield
    # 7. 关闭 Zvec
    # 8. 关闭 Redis 连接
```

---

## 5. 重构对照表（旧项目 vs 新设计）

> 数据来源: Decision-Snapshot 快照（2026-04-16），基于代码事实。

### 5.1 旧项目真实状态

| 指标 | 旧项目 |
|------|--------|
| 后端 | 68 文件 / 10,400 行（含测试 1,878 行） |
| 前端 | 25 文件 / 3,714 行 |
| API 端点 | 22 个 |
| 测试覆盖 | 0（8 个测试文件但未覆盖核心链路） |
| 架构 | 双架构并存（modules/ 新 + services_archive/ 旧） |

### 5.2 架构变更对照

| 维度 | 旧代码 | 新设计 | 变更程度 | 影响 |
|------|--------|--------|----------|------|
| 向量库 | Qdrant（分布式，每论文独立 Collection） | Zvec（嵌入式，单 Collection + Payload） | **重写** | `stores/qdrant_store.py` 废弃，新写 `zvec_store.py` |
| 对话存储 | SQLite（messages 表） | Redis（LIST + TTL + 滑动窗口） | **重写** | `sqlite_repo.py` 中对话部分废弃，新写 `redis_cache.py` |
| 检索架构 | 单路改写 → Embedding → Qdrant → Rerank | 三层：Source Understanding → Channel Query → Fusion | **重写** | `retrieval/service.py` + `query_rewrite_service.py` 废弃，新写 `pipelines/retrieval/` 四个文件 |
| 数据格式 | JSON only（抛弃 Markdown） | Markdown 为主 + JSON 元数据 | **重写** | `cleaning.py` 清洗逻辑需扩展 MD 处理 |
| LLM 客户端 | 双份并存（LLMClient + KimiLLMClient，后者伪流式） | 统一：DeepSeek 对话 + Kimi 仅 VLM | **重构** | 废弃 `KimiLLMClient`，`llm_client.py` 统一走 OpenAI 兼容 |
| MinerU 客户端 | 两份重复（clients/ 旧版 + modules/ingestion/ 新版） | 统一一份 | **清理** | 删除 clients/ 下旧版，保留 ingestion/ 新版 |
| BM25 | `bm25_repo.py`（jieba + BM25Okapi，pickle 持久化） | 沿用，纳入三层架构的 Sparse 通道 | **扩展** | 基础可复用，需适配新 fusion 接口 |
| 切块 | `chunker.py`（32k/24k/重叠策略） | 沿用核心逻辑，扩展 MD 支持 | **迁移** | 核心算法可复用 |
| VLM 图片描述 | `describer.py`（Semaphore=10 并发 + JSON 缓存） | 沿用，标签形式附加到 Markdown | **迁移** | 调用逻辑可复用，输出格式改标签 |
| 前端组件 | 完整（对话/布局/覆盖层 9 组件） | 大部分可复用 | **微调** | SSE 客户端、WPS 轮询、对话 store 基本可用 |

### 5.3 旧项目高危问题（重构必须解决）

| # | 问题 | 快照来源 | 新设计如何解决 |
|---|------|----------|---------------|
| 1 | MinerU 同步轮询阻塞 FastAPI 事件循环，导入期间服务冻结最长 300s | 5-哪里会断 | MinerU 调用改为真正异步（httpx async），导入流程后台执行 |
| 2 | 前后端 API 端点不对齐，知识库管理功能完全无法工作 | 4-答应了什么 | 新 API 设计统一前后端契约，15 个端点精简明确 |
| 3 | Embedding 429 递归重试无上限，持续限流时栈溢出 | 5-哪里会断 | `retry.py` 加最大重试次数 + 指数退避上限 |
| 4 | API Key 为空字符串时启动不报错，运行时才 401 崩溃 | 5-哪里会断 | `config.py` 启动时校验 Key 非空，失败即报错退出 |
| 5 | BM25 降级返回空内容，RAG 问答变"盲答" | 5-哪里会断 | 降级策略：BM25 失败时返回降级标记，不返回空结果 |
| - | KimiLLMClient.chat_stream 是伪流式（先完整请求再 50 字符分块） | 2-做什么 | 废弃 KimiLLMClient，统一用 DeepSeek 真流式 |
| - | QdrantStore 每次 API 请求新建实例 | 5-哪里会断 | Zvec 在 lifespan 中初始化一次，全局复用 |
| - | 两套 LLM 客户端并存 | 1-有什么 | 职责拆分：LLM（DeepSeek）+ VLM（Kimi），各一个文件 |
| - | 0 测试覆盖 | 4-答应了什么 | MVP 先跑通，关键路径稳定后补测试 |

### 5.4 可直接复用的旧代码清单

| 旧文件 | 行数 | 复用方式 | 新位置 |
|--------|------|----------|--------|
| `processing/chunker.py` | ~400 | 核心切块逻辑迁移 | `pipelines/ingestion/chunker.py` |
| `processing/describer.py` | ~130 | VLM 调用逻辑迁移，输出格式改标签 | `pipelines/ingestion/image_handler.py` |
| `modules/ingestion/mineru_client.py` | ~814 | 整体复用，改异步 | `clients/mineru_client.py` |
| `clients/embedding_client.py` | ~120 | 基本复用 | `clients/embedding_client.py` |
| `clients/rerank_client.py` | ~80 | 暂不复用（MVP 不上 Reranker） | v1.1 |
| `repositories/bm25_repo.py` | ~200 | 基本复用，适配 fusion 接口 | `stores/bm25_store.py` |
| `core/retry.py` | ~50 | 复用，加重试上限 | `core/retry.py` |
| `core/config.py` | ~400 | 大幅精简，去掉 Qdrant 相关 | `core/config.py` |
| 前端 SSE 客户端 | ~500 | 复用 | `services/sse-client.ts` |
| 前端 WPS 轮询 | ~300 | 复用 | `services/wps-host.ts` |
| 前端对话 store | ~650 | 复用，微调 | `stores/conversation.ts` |

### 5.5 必须删除/废弃的旧代码

| 旧文件 | 原因 |
|--------|------|
| `stores/qdrant_store.py` | 替换为 Zvec |
| `services_archive/` 整个目录 | 已归档，无用 |
| `services/query_rewrite_service.py` | 替换为三层检索架构 |
| `modules/retrieval/service.py` | 替换为三层检索架构 |
| `modules/qa/service.py` | 重写 |
| `clients/kimi_client.py` 中的 KimiLLMClient | 废弃，只用 VLM 部分 |
| `clients/mineru_client.py`（旧版） | 重复，删除 clients/ 下的 |
| `modules/session/service.py` | 对话改为 Redis |
| `repositories/sqlite_repo.py` 中对话部分 | 迁移到 Redis，SQLite 只保留元数据 |
| `workflows/` 空目录 | 清理 |
